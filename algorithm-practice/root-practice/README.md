# Root Practice Files

This folder keeps older C/C++ practice files that were previously stored at the repository root.

For a cleaner GitHub profile page, the main profile repository root now keeps only the profile `README.md` and organized folders.

